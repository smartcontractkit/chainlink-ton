// Re-export assertLog from the main contracts to ensure type compatibility
export { assertLog } from '../../../../contracts/tests/Logs'
export { LogTypes } from '../../../../contracts/wrappers/ccip/Logs'
